#pragma once
#include"Plants.h"
class Gooseberry:Plants {
    string name;
    string type;
    bool isEdible;
    bool isEatAnimals;
    int count;
public:
    string getName() { return name; }
    int getCount() { return count; }
    string getType() { return type; }
};
class Raspberry :Plants {
    string name;
    string type;
    bool isEdible;
    bool isEatAnimals;
    int count;
public:
    string getName() { return name; }
    int getCount() { return count; }
    string getType() { return type; }

};
class Raspberry :Plants{
    string name;
    string type;
    bool isEdible;
    bool isEatAnimals;
    int count;
public:
    string getName() { return name; }
    int getCount() { return count; }
    string getType() { return type; }
};